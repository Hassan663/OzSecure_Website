node_modules/
.next/
out/
dist/
.env
.env.local
*.log
.DS_Store

# Local JSON store (created at runtime when MONGODB_URI is not set).
# Anchored to the backend root so it does NOT match src/data/ (seed defaults).
/data/
